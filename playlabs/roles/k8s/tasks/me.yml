---

- name: Install your user
  include: user.yml
  vars:
    user:
      name: '{{ ansible_ssh_user }}'
      roles:
        k8s:

- name: Install your namespace
  include: namespace.yml
  vars:
    namespace:
      name: '{{ ansible_ssh_user }}'
