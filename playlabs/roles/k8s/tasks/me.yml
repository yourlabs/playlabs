---

- name: Install your user
  include: user.yml
  vars:
    user:
      name: '{{ ansible_user }}'
      roles:
        k8s:

- name: Install your namespace
  include: namespace.yml
  vars:
    namespace:
      name: '{{ ansible_user }}'
